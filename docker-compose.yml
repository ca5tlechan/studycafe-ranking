services:
  db:
    image: postgres:17
    container_name: studycafe-db
    environment:
      POSTGRES_DB: studycafe
      POSTGRES_USER: studycafe
      POSTGRES_PASSWORD: studycafe
    ports:
      - "5432:5432"
    volumes:
      - studycafe-pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U studycafe -d studycafe"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  studycafe-pgdata:
